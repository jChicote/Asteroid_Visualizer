class CelestialObjectSelector {
    GetCelestialObjectsInCameraView() {
        // Check that it is in view

        // Check that it does exist within the array of celestial objects.

        // If not in view, remove from array and notify object.

        // If in view, add to array and notify object.
    }

    IsCelestialObjectWithinCameraView() {

    }

    // This is intended to return identifiers of celestial objects that are within the camera view.
    GetCelestialObjectIdentifiers() {

    }
}

export { CelestialObjectSelector };
